{
  "project": "Sentinel",
  "tagline": "An autonomous agent that polices other autonomous agents.",
  "category": "Hackathon project — contract + frontend + autonomous patrol agent",
  "summary": "Operators register AI agents with plain-English mandates on-chain and post a bond. A serverless patrol bot walks public blockchains via Blockscout, detects transactions that contradict a mandate, and files challenges on GenLayer. Five validators independently fetch the transaction and agree on one verdict: VIOLATION slashes the bond, COMPLIANT costs the challenger their stake, INCONCLUSIVE refunds. No administrator decides anything.",
  "runner": "py-genlayer:5jycge4q8k23462jtb0b9fyey1s9qz928sz2nbrd9mg4sxqg2qng",
  "consensus": {
    "axis": "One string: VIOLATION / COMPLIANT / INCONCLUSIVE / RETRY. Nothing else is compared.",
    "why": "docs/PROBE.md §4 measured five fields moving between two fetches of the same transaction seconds apart (confirmations, exchange_rate, has_error_in_internal_transactions, and the nested token.holders_count and token.total_supply). §5 then measured validators DISAGREEING about one round in four on a content digest of the STABLE projection, because Blockscout is a load-balanced cluster whose replicas index at different rates. A digest on the axis would leave a quarter of all challenges unsettleable at random.",
    "digest": "Recorded on every challenge as evidence, and never voted on.",
    "retry": "RETRY is on the axis because it is NOT a verdict: validators must agree a source was transiently unavailable, or one node's bad luck silently becomes everybody's refund.",
    "gates_before_the_model": [
      "transient (5xx/429/0) → RETRY, no state applied",
      "404 → INCONCLUSIVE, deterministic absence, challenger refunded",
      "unparseable 200 → INCONCLUSIVE",
      "binding: the transaction must involve the registered wallet, checked in Python",
      "only then does a model see a fenced, defanged projection"
    ],
    "render_chains": "A 403 on a render chain is RETRY, not a verdict: a validator served an interstitial did not read the evidence it would vote on. PROBE.md §11 measured one validator in five classifying a transaction differently from its peers because Cloudflare challenged it and not them, so challenges on that chain converge less often and settle_stalled is a routine path there."
  },
  "data_sources": {
    "ethereum": "eth.blockscout.com",
    "base": "base.blockscout.com",
    "arbitrum": "arbitrum.blockscout.com",
    "polygon": "polygon.blockscout.com",
    "validators_read": "/api/v2/transactions/{hash} — 10-18 KB, one immutable transaction",
    "patrol_bot_reads": "/api/v2/addresses/{wallet}/transactions — 0.5-1.0 MB, 50 rows, moves constantly",
    "no_query_parameters": "Blockscout 422s unknown query parameters; the brief's ?limit=5 is one of them",
    "robinhood": "robinhoodchain.blockscout.com",
    "bot_checked_hosts": "robinhoodchain.blockscout.com answers a plain GET with a 403 Cloudflare interstitial from validator egress (docs/PROBE.md §10). The contract reads it with gl.nondet.web.render — a real browser — and recovers the status and body from the exception render raises on any non-2xx. The patrol bot, which fetches off chain, only needs a browser User-Agent."
  },
  "artifacts": {
    "build/Sentinel.min.py": {
      "source": "contracts/Sentinel.py",
      "bytes": 52783,
      "sha256": "bdfade01fd94a6edecceb1650f4d00eb8ab8e2244f4a28dedfbb9ea3c9b7cf8d",
      "source_sha256": "52b55730a691b35392fc5f3e20c4c36551bfd15f29485e0963cf8fa4fbc66caa",
      "premangle_bytes": 65016
    }
  },
  "deployments": {
    "studiodev": {
      "network": "studiodev",
      "chain_id": 61997,
      "rpc": "https://studio-dev.genlayer.com/api",
      "explorer": "https://explorer-studio-dev.genlayer.com/address/0x3fc4E5dA7bc0a4c28EF52435aE62606D5aED563e",
      "Sentinel": {
        "address": "0x3fc4E5dA7bc0a4c28EF52435aE62606D5aED563e"
      },
      "owner": "0x95FA3CbC31352Ca771be5f11CA8Ce07F0a125D23",
      "verified": "`python3 tools/verify_onchain.py 0x3fc4E5dA7bc0a4c28EF52435aE62606D5aED563e build/Sentinel.min.py` reports EQUIVALENT: the on-chain code and the local artifact are the same token stream under a bijective renaming of PRIVATE identifiers. They are not byte-identical - the deployed bytes were produced by an earlier run of the same pipeline, before the v0.3.0 migration was rebuilt here - and the public ABI, all 36 methods, is unchanged. The check reports MATCH, EQUIVALENT or DIFFER precisely so this distinction is not papered over.",
      "artifact_bytes_on_chain": 52726,
      "live_state": {
        "read_at": "2026-09-10",
        "source": "get_stats, get_agents_by_chain and get_challenges on the deployed contract",
        "agents_registered": 18,
        "agents_active": 16,
        "total_bonded_text": "11",
        "challenges_filed": 4,
        "challenges_settled": 3,
        "patrols_run": 2,
        "watchers": 2,
        "chains_configured": [
          "ethereum",
          "base",
          "arbitrum",
          "polygon",
          "robinhood"
        ],
        "note": "Three challenges settled VIOLATION on 2026-09-10, all filed and judged by the patrol bot. Challenge 0 against agent 0 (mandate 'Only trade ETH and USDC') found a WFC receipt; challenges 1 and 2 against agent 2 followed. The penalty compounds, so agent 2's bond went 0.5 -> 0.4 -> 0.32 and 0.28 GEN was slashed in total, against 0.14 GEN of bounties. Agents 0 and 2 are SLASHED_OUT because their bonds fell below the 0.5 GEN minimum, which is why 16 of 18 are active. Challenge 3 is PENDING. patrols_run is 2: the first patrols driven against this deployment, after the route learned to attach studio-dev fees.",
        "bond_under_watch_text": "10",
        "violations": 3,
        "total_slashed_text": "0.28",
        "bounties_paid_text": "0.14",
        "agents_by_chain": {
          "ethereum": 7,
          "base": 2,
          "arbitrum": 3,
          "polygon": 3,
          "robinhood": 3
        },
        "agents_by_type": {
          "TRADING": 10,
          "DEFI": 7,
          "CUSTOM": 1
        }
      }
    }
  },
  "note": "Verify with: shasum -a 256 build/Sentinel.min.py and compare against the sha256 below, or `genlayer network set studio-dev` then `python3 tools/verify_onchain.py <address> build/Sentinel.min.py`.",
  "build": {
    "command": "bash tools/build.sh",
    "stages": [
      "tools/minify_contract.py -> build/Sentinel.premangle.py",
      "tools/mangle_names.py -> build/Sentinel.min.py + build/Sentinel.names.json"
    ],
    "size_ceiling_measured": {
      "tool": "test/size_gate.py - deploys a padded contract to a live network and reads a value back",
      "accepted": [
        51257,
        51692,
        52400,
        53000
      ],
      "refused": [
        53500
      ],
      "ceiling": "between 53,000 and 53,500 bytes; 53,000 is the largest size PROVEN to deploy",
      "note": "Re-measured when the profile fields were added, rather than assumed to still hold."
    },
    "never_renamed": [
      "the contract class name",
      "public method names (the ABI)",
      "dunders",
      "names used as keyword arguments",
      "names in getattr strings",
      "every string literal"
    ]
  },
  "probe": {
    "contract": "contracts/_render_probe.py",
    "note": "A throwaway diagnostic, run before the contract was written. Not deployed on studiodev.",
    "findings": "docs/PROBE.md - nine findings, three of which changed the architecture"
  },
  "frontend": {
    "url": "https://sentinel-tau-ashen.vercel.app",
    "network": "studiodev",
    "pages": [
      "/",
      "/register",
      "/agents",
      "/agent/[id]",
      "/challenge/[id]",
      "/patrol",
      "/leaderboard",
      "/analytics",
      "/docs"
    ],
    "api": {
      "/api/patrol": "the Sentinel itself — reads the queue, fetches Blockscout, files challenges",
      "/api/txs": "server-side Blockscout proxy for the agent detail page",
      "/api/rpc": "same-origin RPC relay for Studio, whose 429s drop CORS headers"
    },
    "cron": "vercel.json schedules /api/patrol at */10 * * * * on the Pro plan. A patrol run on 2026-09-10 resolved 1 challenge, filed 3 and stamped 2 agents in 241s against studio-dev, so patrols_run is 1 rather than 0. Vercel has still NOT been observed to DELIVER a cron slot on its own — that run was driven by hand — so use an external scheduler against the alias with PATROL_SECRET until a slot is seen to land. See frontend/CRON.md.",
    "gates": "tsc --noEmit, eslint --max-warnings=0 and next build all clean; every route returns 200.",
    "note": "This is the project's production domain. A custom alias was tried and removed: Vercel served it behind SSO deployment protection (302 to vercel.com/sso-api), which would have looked like a dead link.",
    "structure": "Next.js route groups: (marketing) has NO wallet control and names no network; (app) carries the wallet and the network badge. The split is structural, so the landing page has no import path to a wallet prompt at all.",
    "contract": "0x3fc4E5dA7bc0a4c28EF52435aE62606D5aED563e",
    "fees": "studio-dev runs with the fee policy ENABLED (getCurrentFeePolicy().enabled is true) and exposes no feeManagerContract, so the SDK derives the deposit from the local round-fee calculation. Every write - challenge_agent, mark_patrolled, resolve_challenge in the patrol route, and register/challenge/top-up from the browser - estimates with estimateTransactionFeesForWrite against the real calldata and passes the result as `fees`. A write with no fees is a zero-fee transaction and the consensus contract refuses it, which is what stopped the earlier runs. Measured deposits: 0.00061 GEN for register_agent and mark_patrolled, 0.00077 GEN for resolve_challenge and a challenge with a long reason. The patrol wallet must hold GEN for these plus the 0.05 GEN stake per challenge; the route checks its balance once per run and says so in `notes` when it is empty. Fund it on a Studio network with the sim_fundAccount RPC method."
  },
  "verification": {
    "offline_suite": {
      "tests": 410,
      "failed": 0,
      "source": "test/test_logic.py",
      "note": "includes the mangled artifact driven through a full lifecycle, and the profile battery. Re-run green after the v0.3.0 migration."
    },
    "patrol_suite": {
      "tests": 30,
      "failed": 0,
      "source": "test/test_patrol.mjs",
      "note": "the bot's accusation rules against real Blockscout fixtures"
    },
    "live_suite": {
      "checks": 79,
      "failed": 0,
      "source": "test/e2e.mjs",
      "note": "MEASURED ON AN EARLIER NETWORK, NOT RE-RUN ON studiodev. Re-run with `node test/e2e.mjs --network=studiodev --address=0x3fc4E5dA7bc0a4c28EF52435aE62606D5aED563e`."
    },
    "functional_sweep": {
      "methods": 36,
      "failed": 0,
      "source": "test/verify_methods.mjs",
      "note": "37 reported checks: 36 methods plus the balance invariant. MEASURED ON AN EARLIER NETWORK, NOT RE-RUN ON studiodev. Re-run with `node test/verify_methods.mjs --network=studiodev --address=0x3fc4E5dA7bc0a4c28EF52435aE62606D5aED563e`."
    },
    "repository_audit": {
      "checks": 64,
      "failed": 0,
      "skipped": 1,
      "source": "tools/audit.sh",
      "note": "Checks the live Studio Dev contract and the live Vercel site, not local files alone - including the profile fields, get_agents_by_type, that the register spans the configured chains, and /api/check's CORS, 400 and verdict behaviour. Re-measured 2026-09-10 after the roster was seeded and the patrol route learned studio-dev fees: 64 passed, 0 failed, 1 skipped. The one skip is genvm-lint being unable to load the pinned runner from its local cache, so it prints no ABI count; the ABI is counted from the artifact's AST instead, and is 36. The four state-dependent skips recorded earlier are now real passes - the register spans all five chains, three challenges are settled and three violations are proven on chain."
    },
    "rejection_checklist": {
      "checks": 19,
      "failed": 0,
      "source": "tools/checklist.py",
      "note": "AST-checked: forgery, content hash, recomputation, owner powers, refund-on-reject, URL derivation, binding, closures, floats"
    },
    "adversarial_suite": {
      "checks": 99,
      "failed": 0,
      "source": "test/edge_cases.mjs",
      "note": "The rejection, pause, compounding-slash and state-consistency battery. MEASURED ON AN EARLIER NETWORK, NOT RE-RUN ON studiodev. Re-run with `node test/edge_cases.mjs --network=studiodev --address=0x3fc4E5dA7bc0a4c28EF52435aE62606D5aED563e`."
    }
  },
  "features": {
    "agent_profiles": {
      "fields": "agent_name (100), agent_type (TRADING/DEFI/SHOPPING/CONTENT/CUSTOM), description (500), operator_url (200)",
      "all_optional": true,
      "note": "Descriptive only: none of it reaches the judgement prompt and none of it can move money. The operator URL is scheme-restricted to http/https ON CHAIN, because the frontend renders it as a link and a javascript: href stored here would be a stored XSS for every visitor.",
      "new_view": "get_agents_by_type(agent_type, count)"
    },
    "public_compliance_api": {
      "route": "/api/check?wallet=0x…&chain=ethereum",
      "auth": "none — everything it returns is already public on chain",
      "cors": "open, so it can be called from a browser",
      "note": "Returns {registered:false} for an unknown wallet. For a known one it returns the mandate, the compliance score in both bps and percent, the bond, and the last five verdicts — plus `untested`, which says plainly when a 100% score means 'never tested' rather than 'tested and clean'."
    },
    "fifth_chain": {
      "chain": "robinhood",
      "explorer": "robinhoodchain.blockscout.com",
      "why_it_is_not_a_table_edit": "The brief was 'same Blockscout API format as existing chains'. The schema is the same; the access path is not. Adding the host to CHAIN_HOSTS alone would have shipped a chain where every challenge settled INCONCLUSIVE forever, because a 403 falls through _judge's status != 200 gate — silent, permanent, and indistinguishable from a register full of well-behaved agents.",
      "contract": "RENDER_CHAINS + _http_render + a render-aware _transient",
      "frontend": "chain selector, /agents filter, ChainTag badge #9945FF, chart series, native ETH aliases",
      "patrol": "browser User-Agent on every explorer fetch, and 403 treated as transient",
      "known_limits": [
        "Cloudflare decides per request, so validators can disagree about a transaction none of them dispute (PROBE.md §11)",
        "absence is not deterministic: the same missing hash returned 404 and then 500 (PROBE.md §10)",
        "four of nine live wallets return a repeated 500 on the address-transaction endpoint and cannot be patrolled at all"
      ]
    }
  },
  "runner_note": "The v0.3.0 runner. The contract header is TWO lines - `# v0.3.0` then the Depends pin - and the SDK surface moved with it: gl.contract.Contract, gl.storage.allow, gl.storage.TreeMap / gl.storage.DynArray, gl.message.raw."
}
